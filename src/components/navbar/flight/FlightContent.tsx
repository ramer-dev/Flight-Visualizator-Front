import styled from "@emotion/styled";

const FlightContainer = styled.div`
    width:100%;
`

const FlightContent = () => {
    return (
        <FlightContainer>
            

        </FlightContainer>
    )
}

export default FlightContent;